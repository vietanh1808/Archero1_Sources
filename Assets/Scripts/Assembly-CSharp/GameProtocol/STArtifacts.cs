using System.IO;

namespace GameProtocol
{
	public sealed class STArtifacts
	{
		public bool m_nChange;

		public CArtifact[] m_vecArtifactData;

		public void ReadFromStream(BinaryReader reader)
		{
		}

		public void WriteToStream(BinaryWriter writer)
		{
		}
	}
}
